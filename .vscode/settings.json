{
  "markdownlint.config": {
    "MD025": false, // Multiple top level headers in the same document
    "MD033": false, // Inline HTML
    "MD034": false, // Bare URLs for `Media` usage
    "MD036": false, // Emphasis used instead of a heading
  }
}
